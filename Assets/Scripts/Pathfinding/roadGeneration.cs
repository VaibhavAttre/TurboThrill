using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class roadGeneration : MonoBehaviour
{
    /*
    float[,] terrainMap;
    Vector3 startPoint;
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    private Vector3[] GeneratePath()
    {
        Vector3[] path = new Vector3[0];
    }

    private List<Vector3> GetNeighbors(Vector3 currPoint)
    {
        List<Vector3> neighbors = new List<Vector3>();

        for(int x = -1; x < 1; x++)
        {
            for(int z = -1; z < 1; z++)
            {
                float xCoord = currPoint.x + x;
                float zCoord = currPoint.z + z;
                Vector3 neighbor = new Vector3(xCoord, 0, zCoord);
                neighbors.Add(neighbor);
            }
        }

        return neighbors;
    }

    private float getTotalDistance(Vector3 currPoint)
    {
        return Vector3.Distance(currPoint, startPoint);
    }*/
}
